version: "3.8"

services:
  costcontrol:
    build: .
    ports:
      - "8600:8600"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
    environment:
      - COSTCONTROL_PORT=8600
    restart: unless-stopped
